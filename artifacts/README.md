# Artifacts Directory

This directory stores local experiment outputs (SQLite traces, metrics, plots, checkpoints, logs snapshots).

By default, heavy files under this folder are excluded from Git via `.gitignore`.
